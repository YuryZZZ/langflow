name: Jules Bridge

on:
  issues:
    types: [opened, edited]
  workflow_dispatch:
    inputs:
      task:
        description: 'Task for Jules'
        required: true

jobs:
  dispatch_jules:
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'issues' && contains(github.event.issue.labels.*.name, 'jules'))
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch to Jules
        env:
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
          TASK: ${{ github.event.inputs.task || github.event.issue.body }}
        run: |
          echo "Dispatching task to Jules: $TASK"
          curl -X POST "https://jules.googleapis.com/mcp/" \
            -H "Authorization: Bearer $JULES_API_KEY" \
            -H "Content-Type: application/json" \
            -d "{\"repository\": \"${{ github.repository }}\", \"task\": \"$TASK\"}"